name: Keep API awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Render API
        run: curl -fsS --retry 5 --retry-delay 5 --max-time 30 https://auth-under-the-hood.onrender.com/api/health
